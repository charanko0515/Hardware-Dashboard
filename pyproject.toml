[project]
name = "final-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "manoel", email = "manoel@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
final-project = "final_project:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
